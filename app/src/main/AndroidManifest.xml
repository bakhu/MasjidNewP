<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bakhu.masjidnewp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RemoteMySQLActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MovieUpdateDeleteActivity"
            android:label="@string/title_activity_movie_update_delete"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MovieListingActivity"
            android:label="@string/title_activity_add_movie"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AddMovieActivity"
            android:label="@string/add_new"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>